---
// src/layouts/SiteLayout.astro
import TicketBanner from '../components/site/TicketBanner.astro';
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Affiliations from "../components/site/Affiliations.astro";

type Props = {
  title: string;
  description: string;
  lang?: string;
  showAffiliations?: boolean;
  bodyClass?: string;
};

const {
  title,
  description,
  lang = "en",
  showAffiliations = true,
  bodyClass = "",
} = Astro.props as Props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead title={title} description={description} />
    <link rel="icon" type="image/png" href="/ciq_logo_small_scaled_rounded.png" />
  </head>
  <body class={bodyClass}>
    <TicketBanner />
    <Header id="siteHeader"/>

    <main id="main-content">
      <slot />
    </main>
    {showAffiliations && <Affiliations />}
    <Footer />
  </body>
</html>
