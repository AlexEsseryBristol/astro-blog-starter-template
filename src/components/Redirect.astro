---
type Props = {
  to: string;
  title?: string;
};

const { to, title = "Redirecting…" } = Astro.props as Props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0; url=${to}`} />
    <link rel="canonical" href={to} />
    <title>{title}</title>
  </head>
  <body>
    <p>
      Redirecting to <a href={to}>{to}</a>.
    </p>
  </body>
</html>
